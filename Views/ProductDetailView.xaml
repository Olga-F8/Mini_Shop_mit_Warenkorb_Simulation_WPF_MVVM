<UserControl x:Class="Mini_Shop_mit_Warenkorb_Simulation_WPF.Views.ProductDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mini_Shop_mit_Warenkorb_Simulation_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Produktname -->
        <TextBlock Text="{Binding Product.Name}"
                   FontSize="26"
                   FontWeight="Bold"/>
        <!--Image-->
        <Image Grid.Row="1" Width="100" Height="100" 
       Source="{Binding Product.ImageUrl}" HorizontalAlignment="Left" />

        <!-- Preis -->
        <TextBlock Grid.Row="2"
                   Text="{Binding Product.Price, StringFormat='Preis: {0} €'}"
                   FontSize="18"
                   Margin="0,10,0,0"/>

        <!-- Beschreibung (optional falls vorhanden) -->
        <TextBlock Grid.Row="4"
                   Text="{Binding Product.Description}"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"/>

        <!-- Steuerung -->
        <StackPanel Grid.Row="5"
                    Margin="0,40,0,0"
                    Orientation="Vertical">

            <!-- Menge -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                <TextBlock Text="Menge:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <Button Content="-"
                        Width="30"
                        Command="{Binding DecreaseQuantityCommand}" />

                <TextBlock Text="{Binding Quantity}"
                           Width="40"
                           TextAlignment="Center"
                           VerticalAlignment="Center"/>

                <Button Content="+"
                        Width="30"
                        Command="{Binding IncreaseQuantityCommand}" />

            </StackPanel>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal"
                        Margin="0,20,0,0">

                <Button Content="In Warenkorb"
                        Width="150"
                        Margin="0,0,10,0"
                        Command="{Binding AddToCartCommand}" />

                <Button Content="Zurück"
                        Width="100"
                            Margin="0,0,10,0"
                        Command="{Binding BackCommand}" />



            </StackPanel>

            <TextBlock Text="{Binding Message}"
                Foreground="Green"
                FontWeight="Bold"
                Margin="0,10,0,0"/>

        </StackPanel>

    </Grid>

</UserControl>